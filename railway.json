{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "PYTHONPATH=/app/backend uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
